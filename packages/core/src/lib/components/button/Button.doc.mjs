// @generated by docs/scripts/emit-core-docs.mjs — do not edit.
//
// Prose is upstream's, verbatim. Every `type` is read from this package's own
// compiled declarations rather than upstream's React ones, which is why this
// file is generated instead of copied.

export default {
	type: 'component',
	name: 'Button',
	displayName: 'Button',
	keywords: [
		'button',
		'btn',
		'cta',
		'submit',
		'action',
		'loading',
		'primary',
		'secondary',
		'ghost',
		'destructive',
		'danger'
	],
	group: 'Button',
	category: 'Action',
	theming: {
		targets: [
			{
				className: 'astryx-button',
				visualProps: ['size', 'variant']
			}
		],
		vars: [
			{
				name: '--_button-radius',
				description: 'Border radius',
				default: 'var(--radius-element)',
				private: true
			},
			{
				name: '--button-focus-offset',
				description: 'Focus ring outline offset',
				default: 'var(--focus-outline-offset)'
			},
			{
				name: '--button-icon-only-aspect',
				description: 'Aspect ratio for icon-only buttons',
				default: '1 / 1'
			}
		],
		derived: [
			{
				property: 'borderRadius',
				vars: ['--_button-radius']
			}
		]
	},
	usage: {
		description:
			'Button triggers an action when clicked. Use it for form submissions, confirmations, navigation, or any interaction that needs a clear call to action.',
		bestPractices: [
			{
				guidance: true,
				description:
					'Reserve primary for the single most important action in the view. Use secondary or ghost for everything else based on emphasis.'
			},
			{
				guidance: true,
				description:
					'Write labels that describe the action ("Save changes", "Delete account", "Send invite"), not vague labels like "OK" or "Click here".'
			},
			{
				guidance: true,
				description:
					'Show a loading state for actions that take time, like saving or submitting, so the user knows it is working.'
			},
			{
				guidance: true,
				description:
					'Always provide a label for icon-only buttons so screen readers can announce what the button does. Add a tooltip for sighted users.'
			},
			{
				guidance: true,
				description:
					"For a dedicated icon-only button, use IconButton. It is a separate component from Button, exported from '@astryx-svelte/core' alongside it."
			},
			{
				guidance: false,
				description:
					'Place more than one primary button in the same view; this dilutes the visual hierarchy.'
			},
			{
				guidance: false,
				description:
					'Use the destructive variant without a confirmation step for irreversible actions like deleting data.'
			},
			{
				guidance: false,
				description:
					'Use a button for navigation. If it only takes the user to another page, use a link instead. Buttons are for actions like saving, deleting, or submitting.'
			}
		],
		anatomy: [
			{
				name: 'Icon',
				required: false,
				description:
					'A leading icon that reinforces the label, like a trash icon on a Delete button.'
			},
			{
				name: 'Label',
				required: true,
				description: 'The visible text describing the action. Also used as the accessible name.'
			},
			{
				name: 'End content',
				required: false,
				description:
					'A trailing badge or icon after the label, like a notification count or dropdown arrow.'
			},
			{
				name: 'Spinner',
				required: false,
				description: 'Replaces the icon during loading to show the action is in progress.'
			}
		]
	},
	playground: {
		defaults: {
			label: 'Click me',
			variant: 'primary'
		}
	},
	props: [
		{
			name: 'label',
			type: 'string',
			description:
				'Accessible label. Rendered as visible text by default; used as aria-label when isIconOnly is true.',
			required: true
		},
		{
			name: 'variant',
			type: "'primary' | 'secondary' | 'ghost' | 'destructive'",
			description: 'Visual style variant.',
			default: "'secondary'"
		},
		{
			name: 'size',
			type: "'sm' | 'md' | 'lg'",
			description: 'Size variant.',
			default: "'md'"
		},
		{
			name: 'elevation',
			type: "'none' | 'low' | 'med' | 'high'",
			description:
				'Resting shadow depth for floating buttons (e.g. a FAB). `none` is the default flat button; `low`/`med`/`high` map to the shadow token scale. Ignored inside a ButtonGroup, where elevation is owned by the group.',
			default: "'none'"
		},
		{
			name: 'type',
			type: "'submit' | 'reset' | 'button'",
			description: 'HTML button type attribute.',
			default: "'button'"
		},
		{
			name: 'name',
			type: 'string | null',
			description: 'HTML name attribute for form submission.'
		},
		{
			name: 'value',
			type: 'string | string[] | number | null',
			description: 'HTML value attribute for form submission.'
		},
		{
			name: 'form',
			type: 'string | null',
			description: 'Associates the button with a form element by ID.'
		},
		{
			name: 'isLoading',
			type: 'boolean',
			description:
				'Shows a loading spinner and disables interaction. Announces "Loading" via a live region.',
			default: 'false'
		},
		{
			name: 'isInterruptible',
			type: 'boolean',
			description:
				'Keep the button clickable while a clickAction is pending: the spinner and aria-busy still show, but the button is not disabled and the action is not deduped, so a re-click lands and interrupts the in-flight action with a fresh one.',
			default: 'false'
		},
		{
			name: 'isDisabled',
			type: 'boolean',
			description:
				'Disables the button. When a tooltip is present, uses aria-disabled instead of native disabled so the button stays focusable.',
			default: 'false'
		},
		{
			name: 'icon',
			type: 'Snippet',
			description: 'Icon element rendered before the label text.'
		},
		{
			name: 'isIconOnly',
			type: 'boolean',
			description:
				'When true, renders as a square icon-only button with label as aria-label. Requires icon. Tip: for a dedicated icon-only button component, use IconButton instead.',
			default: 'false'
		},
		{
			name: 'width',
			type: 'string | number',
			description:
				"Width of the button. Numbers are treated as pixels, strings are used as-is (e.g., '100%' for a full-width button). By default the button sizes to its content."
		},
		{
			name: 'children',
			type: 'Snippet',
			description:
				'Optional override for visible text. When provided, displayed instead of label, but label is still required (it provides the accessible name). For most cases, just use label alone: <Button label="Save" />.'
		},
		{
			name: 'endContent',
			type: 'Snippet',
			description:
				'Trailing icon or badge rendered after the label. Ignored when isIconOnly is true. Color is inherited from the button variant.'
		},
		{
			name: 'tooltip',
			type: 'string',
			description: 'Tooltip text shown on hover.'
		},
		{
			name: 'onclick',
			type: 'MouseEventHandler<T> | null',
			description: 'Standard click handler (passed through from ButtonHTMLAttributes).'
		},
		{
			name: 'clickAction',
			type: '(event: MouseEvent) => unknown',
			description: 'Async click handler. Shows loading state while the returned promise is pending.'
		}
	]
};
