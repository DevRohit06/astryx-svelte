// @generated by docs/scripts/emit-core-docs.mjs — do not edit.
//
// Prose is upstream's, verbatim. Every `type` is read from this package's own
// compiled declarations rather than upstream's React ones, which is why this
// file is generated instead of copied.

export default {
	type: 'component',
	name: 'Toolbar',
	displayName: 'Toolbar',
	keywords: [
		'toolbar',
		'nav',
		'bar',
		'actions',
		'buttonbar',
		'header',
		'footer',
		'action-bar',
		'control-bar'
	],
	category: 'Action',
	theming: {
		targets: [
			{
				className: 'astryx-toolbar',
				states: ['size']
			}
		]
	},
	usage: {
		description:
			'Toolbar is a horizontal bar with left, center, and right areas. Use it for contextual actions within a content area (above a table, inside a card, or in a panel), not as a page-level header. Set the size once on the toolbar and all buttons, inputs, and tabs inside it match automatically.',
		bestPractices: [
			{
				guidance: true,
				description:
					'Put secondary actions like "Back" on the left, and primary actions like "Save" on the right.'
			},
			{
				guidance: true,
				description:
					"Make temporary toolbars like bulk selection visually distinct so users can tell they're contextual, for example with a background color or border."
			},
			{
				guidance: true,
				description:
					'Visually separate the toolbar from the content below it, with a divider, a background variant, or both.'
			},
			{
				guidance: true,
				description:
					'Use Toolbar as a card header when the header has interactive actions like filter or add; it gives you slot layout, keyboard navigation, and size cascading. If the header is just a title with no actions, a LayoutHeader or Section is enough.'
			},
			{
				guidance: false,
				description: 'Put too many actions in one toolbar; move less common items into a MoreMenu.'
			},
			{
				guidance: false,
				description:
					'Set size on individual child buttons; set it once on the toolbar and it cascades automatically.'
			},
			{
				guidance: false,
				description:
					'Use Toolbar for app-wide navigation like main menu links or sign out; use TopNav or LayoutHeader for that.'
			}
		]
	},
	playground: {
		defaults: {
			label: 'Table actions',
			size: 'sm',
			dividers: ['bottom']
		}
	},
	props: [
		{
			name: 'label',
			type: 'string',
			description: 'Accessible label for the toolbar, applied as aria-label.',
			required: true
		},
		{
			name: 'startContent',
			type: 'Snippet',
			description: 'Content aligned to the start (left in LTR).'
		},
		{
			name: 'centerContent',
			type: 'Snippet',
			description: 'Centered content. Switches layout to CSS grid (1fr auto 1fr).'
		},
		{
			name: 'endContent',
			type: 'Snippet',
			description: 'Content aligned to the end (right in LTR).'
		},
		{
			name: 'size',
			type: "'sm' | 'md' | 'lg'",
			description:
				'Size of the toolbar. Controls minimum height and coordinates with Button, TextInput, TabList, and Selector; children inherit this size as their default via SizeContext.',
			default: "'md'"
		},
		{
			name: 'gap',
			type: '0 | 0.5 | 1 | 1.5 | 2 | 3 | 4 | 5 | 6 | 8 | 10',
			description: 'Gap between items within each slot.',
			default: '1'
		},
		{
			name: 'orientation',
			type: "'horizontal' | 'vertical'",
			description: 'Orientation for keyboard navigation. Controls arrow key direction.',
			default: "'horizontal'"
		},
		{
			name: 'variant',
			type: "'section' | 'transparent' | 'muted'",
			description: 'Visual variant passed to Section.',
			default: "'transparent'"
		},
		{
			name: 'dividers',
			type: 'SectionDivider[]',
			description: 'Borders/dividers rendered around the toolbar.'
		},
		{
			name: 'xstyle',
			type: 'StyleArg',
			description: 'StyleX styles for layout customization. Must be a stylex.create() value.'
		}
	]
};
