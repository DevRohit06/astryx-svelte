// @generated by docs/scripts/emit-core-docs.mjs — do not edit.
//
// Prose is upstream's, verbatim. Every `type` is read from this package's own
// compiled declarations rather than upstream's React ones, which is why this
// file is generated instead of copied.

export default {
	type: 'component',
	name: 'Lightbox',
	displayName: 'Lightbox',
	keywords: [
		'lightbox',
		'image',
		'video',
		'viewer',
		'gallery',
		'zoom',
		'fullscreen',
		'media',
		'photo',
		'preview'
	],
	category: 'Overlay',
	theming: {
		targets: [
			{
				className: 'astryx-lightbox',
				visualProps: []
			}
		]
	},
	usage: {
		description:
			'A fullscreen overlay for viewing images and videos at full resolution. Supports single-item and gallery modes with prev/next navigation, optional zoom and pan for images, and native video controls.',
		bestPractices: [
			{
				guidance: true,
				description: 'Always provide alt text for every image for screen reader accessibility.'
			},
			{
				guidance: true,
				description: 'Use gallery mode with onIndexChange for multi-image sets.'
			},
			{
				guidance: true,
				description:
					'Enable hasZoom only when viewing high-resolution images that benefit from close inspection.'
			},
			{
				guidance: false,
				description: 'Use the lightbox for non-image content; it is specialized for images.'
			},
			{
				guidance: false,
				description: 'Nest interactive content inside captions; keep them plain text.'
			}
		]
	},
	playground: {
		defaults: {
			isOpen: false,
			media: {
				src: 'https://lookaside.facebook.com/assets/astryx/light-scene-horizontal-1.png',
				alt: 'Coastal shoreline with ocean waves',
				caption: 'A scenic coastline with waves rolling onto a sandy beach beneath a clear sky.'
			}
		},
		overlay: true
	},
	props: [
		{
			name: 'isOpen',
			type: 'boolean',
			description: 'Whether the lightbox is open.',
			required: true
		},
		{
			name: 'onOpenChange',
			type: '(isOpen: boolean) => void',
			description: 'Callback when the lightbox open state changes.',
			required: true
		},
		{
			name: 'media',
			type: 'LightboxMedia | LightboxMedia[]',
			description:
				"Media to display. Pass a single object for one item, or an array for gallery mode with prev/next navigation. Each item is {src: string, alt: string, caption?: ReactNode, type?: 'image' | 'video'}; type defaults to 'image', and zoom/pan is disabled for 'video'.",
			required: true
		},
		{
			name: 'index',
			type: 'number',
			description: 'Current index in gallery mode (when media is an array).'
		},
		{
			name: 'onIndexChange',
			type: '(index: number) => void',
			description: 'Callback when the gallery index changes via prev/next navigation.'
		},
		{
			name: 'hasZoom',
			type: 'boolean',
			description:
				'Enable zoom on double-click, or Enter/Space/+/- via keyboard (images only). When zoomed, drag or use arrow keys to pan.',
			default: 'false'
		},
		{
			name: 'xstyle',
			type: 'StyleArg',
			description: 'StyleX styles for layout customization. Must be stylex.create() value.'
		}
	]
};
