// @generated by docs/scripts/emit-core-docs.mjs — do not edit.
//
// Prose is upstream's, verbatim. Every `type` is read from this package's own
// compiled declarations rather than upstream's React ones, which is why this
// file is generated instead of copied.

export default {
	type: 'component',
	name: 'MobileNav',
	displayName: 'Mobile Nav',
	keywords: [
		'mobilenav',
		'drawer',
		'sidebar',
		'navigation',
		'hamburger',
		'menu',
		'offcanvas',
		'slideout',
		'navdrawer',
		'toggle'
	],
	group: 'Navigation',
	category: 'Navigation',
	theming: {
		targets: [
			{
				className: 'astryx-mobile-nav',
				visualProps: ['side']
			}
		]
	},
	usage: {
		description:
			'A slide-out drawer for mobile navigation. MobileNav is the mobile counterpart to SideNav and accepts the same children. Use it on narrow viewports where a persistent sidebar is not practical. Inside AppShell, use MobileNavToggle as the trigger; it reads state from context automatically.',
		bestPractices: [
			{
				guidance: true,
				description:
					'Share the same nav items between MobileNav and SideNav by extracting them into a variable.'
			},
			{
				guidance: true,
				description: "Provide a header when the drawer's purpose is not obvious from its content."
			},
			{
				guidance: true,
				description:
					'Inside AppShell, use MobileNavToggle to open the drawer; it reads state from context. Do not pass isOpen/onOpenChange to the toggle.'
			},
			{
				guidance: false,
				description: 'Use MobileNav on desktop: use a persistent SideNav instead.'
			}
		]
	},
	playground: {
		defaults: {
			isOpen: false,
			header: 'Navigation'
		},
		overlay: true
	},
	props: [
		{
			name: 'isOpen',
			type: 'boolean',
			description:
				'Whether the drawer is open. Inside AppShell, this is managed automatically via context. Outside AppShell, provide this prop to control the drawer yourself.'
		},
		{
			name: 'onOpenChange',
			type: '(isOpen: boolean) => void',
			description:
				'Called when the drawer visibility changes (backdrop click, Escape key, or close button). Inside AppShell, this is managed automatically via context.'
		},
		{
			name: 'children',
			type: 'Snippet',
			description: 'Drawer content: typically SideNavSection/SideNavItem, or any ReactNode.',
			required: true
		},
		{
			name: 'header',
			type: 'string | Snippet',
			description:
				'Header content for the drawer. Rendered next to the close button. Pass a string for a simple text heading, or a ReactNode for custom content (logo, search bar, etc.).'
		},
		{
			name: 'width',
			type: 'number',
			description: 'Drawer width in pixels. Capped at 85vw to prevent overflow on small screens.',
			default: '320'
		},
		{
			name: 'side',
			type: "'auto' | 'end' | 'start'",
			description:
				'Which side the drawer slides from. Start is left in LTR, right in RTL. Auto picks a side based on the trigger position.',
			default: "'auto'"
		}
	]
};
