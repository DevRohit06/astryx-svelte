<script lang="ts">
	/**
	 * Upstream's `render(<div />)` for the case that presses Escape with nothing
	 * registered. A fixture because `render()` here takes a component, and the
	 * case is about a mounted page that owns no layer — not about no page at all.
	 */
</script>

<div></div>
