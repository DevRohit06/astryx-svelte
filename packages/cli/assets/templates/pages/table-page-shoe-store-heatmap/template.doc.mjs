/**
 * @file Shoe Sales Table page template. Transcribed from upstream's
 * `assets/templates/pages/table-page-shoe-store-heatmap/page.tsx`.
 *
 * The slug says "heatmap" and the page renders an area chart; upstream's
 * `category` says `Table - Chart` accordingly. Both are upstream's, verbatim.
 */

/** @type {import('@astryx-svelte/cli/authoring').TemplateDoc} */
export const doc = {
	type: 'page',
	name: 'Shoe Sales Table',
	displayName: 'Shoe Sales Table',
	description: 'Shoe store sales data table with a revenue area chart and product thumbnails.',
	isReady: true,
	category: 'Table - Chart',
	isHiddenFromOverview: true
};
