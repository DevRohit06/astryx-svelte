<!--
	Ported from upstream's `templates/blocks/components/Toast/ToastShowcase.tsx`.
	Transcribed, not re-authored: the parity rule covers example content too.
-->
<script lang="ts">
	import ToastShowcase from '../Toast/ToastShowcase.svelte';
</script>

<!--
	Upstream's doc lists this block under `useToast` as well as `Toast`
	(`alsoExampleFor`), and the registry keys a block by component, so the hook's
	page renders the same block rather than a second copy of it.
-->
<ToastShowcase />
